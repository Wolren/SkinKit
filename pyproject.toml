[project]
name = "skinkit"
version = "0.2"
description = "Full QGIS UI customisation plugin"
requires-python = ">=3.8"

[tool.ruff]
target-version = "py38"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
